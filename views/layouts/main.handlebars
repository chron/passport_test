<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Hudson</title>
</head>
<body>
    {{#if user}}
      <h1>{{user.username}} (<a href="/logout">logout</a>)</h1>
    {{else}}
      <a href="/login">login</a>
    {{/if}}
    {{{body}}}
</body>
</html>
